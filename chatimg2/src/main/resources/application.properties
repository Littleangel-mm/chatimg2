spring.application.name=chatimg2

# PostgreSQL
spring.datasource.url=jdbc:postgresql://${DB_HOST:47.108.211.186}:${DB_PORT:5432}/${DB_NAME:chatimg2}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:fl3692458121}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Image backup path
app.image.backup-path=./img/

# API config
app.api.key=${API_KEY:}
app.api.url=${API_URL:https://www.maitokens.com}
app.api.model.text2img=${API_MODEL_TEXT2IMG:gpt-image-2}
app.api.model.img2img=${API_MODEL_IMG2IMG:gpt-image-2}

# Server
server.port=8081
